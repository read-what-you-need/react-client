import React from 'react';

const UnderProcess = () => {
    return (
        <p>Come back after some time. The file is yet to be processed and is in a queue right now.</p>
    );
}

export default UnderProcess;